<div class="col-md-6">
  <div>
  <span>Sincronizar alterações na planilha</span>
  <%= button_to '🔄 Sincronizar Planilha', :sync_confirmations, method: :post, class: "btn btn-primary", data: { confirm: "Tem certeza que deseja sincronizar?" } %>
  </div>
  <h1>Bixes não confirmados</h1>

  <div class="grid">
    <% @non_confirmed.each_with_index do |b, i| %>
      <div class="row list-item-<%= i % 2 %>">
        <h4>
          <%= button_to b.nome, "/bixes/#{b.id}/confirmacoes", method: :post %>
        </h4>
      </div>
    <% end %>
  </div>
</div>
<div class="col-md-6">
  <h1>Bixos confirmados</h1>

  <div class="grid">
    <% @confirmed.each_with_index do |b, i| %>
      <div class="row team-<%= b.team %> team-container">
        <div class="col team-infos">
          <%= link_to b.nome, bixe_path(b.id) %>
          <%= image_tag "#{b.team}.svg", class: "team-logo" %>
        </div>  
        <div class="col">
          <%= button_to "🗑️", confirmacao_path(b.confirmacao.id), method: :delete, data: { confirm: "Tem certeza?" }, id: "deleteBtn"%>
        </div>
      </div>
    <% end %>
  </div>
</div>